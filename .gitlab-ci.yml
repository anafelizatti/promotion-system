stages:
    - build
    - test

build:
    stage: build
    script:
        -echo "Testando CI"
        -mkdir teste
        -touch mkdir/teste.txt

test:
    stage: test
    script:
        - echo "Testando"
        - test -f "build/teste.txt"

        
